<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>
        <%= usuario.nombre %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>

<body>
    <div class="container p-3">
        <div class="card shadow galeria">
            <div class="card-body p-2">
                <div class="card-title d-flex flex-column justify-content-center align-items-center p-2 gap-2">
                    <h1 class="card-title text-center fw-bold">
                        <%= usuario.nombre %>
                            <%= usuario.apellidos %>
                    </h1>

                </div>
                <% if(usuario) {%>
                    <div class="card shadow card">
                        <div class="card-body p-3">
                            <div class="card-title d-flex flex-column justify-content-center align-items-center gap-2">
                                <img src="/uploads/<%= usuario.imagen %>"
                                    alt="Imagen del usuario <%= usuario.nombre %>">
                                <h2 class="text-center ">
                                    <%= usuario.nombre %>
                                        <%= usuario.apellidos %>
                                </h2>
                            </div>
                            <p class="text-start"> Correo: <span class="fw-bold">
                                    <%= usuario.correo %>
                                </span> </p>
                            <div class="d-flex flex-row justify-content-center gap-2 m-2">
                                <a href="/usuarios/" class="btn btn-outline-primary btn-lg" role="button">
                                    Volver</a>
                                <button class="btn btn-danger btn-lg" onclick="eliminarUsuario('nombre')" role="button">
                                    Eliminar </a>
                            </div>
                        </div>
                    </div>
            </div>
            <% } else {%>
                <h2 class="text-center text-danger"> No hay libros ahora mismo en Goma Books! Disculpa las
                    molestías</h2>
                <% } %>


        </div>
    </div>
    </div>
    <script src="/js/bootstrap.bundle.min.js"></script>
    <script src="/js/usuariosAjax.js"></script>
</body>


</html>