<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Formulario de login</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>

<body class="bg-light">
    <div class="container p-3">
        <div class="card shadow">
            <div class="card-body p-3">
                <div class="card-title">
                    <h1 class="text-center">
                        Acceso
                    </h1>
                </div>
                <% if(error !==null) { %>
                    <div class="alert alert-danger alert-dismissible justify-content-space-between fade show"
                        role="alert">
                        <strong>
                            <%= error %>
                        </strong>
                        <button type="button" data-bs-dismiss="alert" class="btn btn-close"></button>
                    </div>
                    <% } %>
                        <!-- De tipo POST porque al final estamos enviando datos importantes como la contraseña -->
                        <form action="/" method="post">
                            <div class="row p-3">
                                <div class="col-12 mb-3">
                                    <label for="login" class="form-label fw-bold">Login:</label>
                                    <input type="text" class="form-control" name="login" id="login"
                                        placeholder="aitor01" maxlength="20" required>
                                </div>
                            </div>
                            <div class="row p-3">
                                <div class="col-12 mb-3">
                                    <label for="password" class="form-label fw-bold">Password:</label>
                                    <input type="password" name="password" id="password" class="form-control"
                                        pattern="^(?=(?:.*\d){2,})(?=(?:.*[A-Z]){3,}).{5,}$" maxlength="30"
                                        title="Se necesitan 2 dígitos, 3 mayúsculas y 5 caracteres como mínimo"
                                        required>
                                </div>
                            </div>
                            <div class="row p-3">
                                <div class="d-flex justify-content-end">
                                    <button type="submit" class="btn btn-primary btn-md">Submit </button>
                                </div>
                            </div>
                        </form>
            </div>
        </div>
    </div>
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>

</html>